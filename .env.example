# Example environment for Clawboard
# Copy to .env and fill in values.

# Shared API token:
# - required for all write endpoints
# - required for non-localhost reads (including Tailscale/custom-domain access)
# - localhost reads are allowed without token
CLAWBOARD_TOKEN=your-token-here

# Optional: frontend default token to prefill Setup/local storage on first load.
# Security warning: this is embedded in the browser bundle; only use on private/trusted networks.
NEXT_PUBLIC_CLAWBOARD_DEFAULT_TOKEN=

# Unified view pagination: topics per page (higher renders more topics per page).
NEXT_PUBLIC_CLAWBOARD_UNIFIED_TOPICS_PAGE_SIZE=50
# Initial /api/changes snapshot cap for logs on page refresh.
# Lower values speed up first paint on large instances; set 0 to request full history.
NEXT_PUBLIC_CLAWBOARD_INITIAL_CHANGES_LIMIT_LOGS=2000
# Keep optimistic "responding" state for request-attributed sends (seconds).
# Set explicitly so long-running Task/Topic chats stay visible across multi-hour/day runs.
NEXT_PUBLIC_OPENCLAW_OPTIMISTIC_REQUEST_TTL_SECONDS=604800
# Keep optimistic "responding" state for pre-queue sends that do not have requestId yet (seconds).
NEXT_PUBLIC_OPENCLAW_OPTIMISTIC_NO_REQUEST_TTL_SECONDS=21600
# Max age for topic->task promotion linkage after a send (seconds).
# Longer windows preserve typing/response continuity when promotion is delayed.
NEXT_PUBLIC_OPENCLAW_PROMOTION_SIGNAL_WINDOW_SECONDS=604800
# Cleanup window for inactive topic->task typing aliases (seconds).
# Active aliases are retained regardless of age until source responding state clears.
NEXT_PUBLIC_OPENCLAW_TYPING_ALIAS_INACTIVE_RETENTION_SECONDS=2592000

# Database (default runtime target: Postgres)
CLAWBOARD_DB_URL=postgresql+psycopg://clawboard:clawboard@db:5432/clawboard
POSTGRES_USER=clawboard
POSTGRES_PASSWORD=clawboard
POSTGRES_DB=clawboard

# Public API base for the web UI.
# IMPORTANT: must be reachable from the *browser*. If you access Clawboard over
# Tailscale, set this to your tailnet IP/hostname (e.g. http://100.x.y.z:8010).
CLAWBOARD_PUBLIC_API_BASE=http://localhost:8010

# Next.js server-side API proxy target.
# In Docker Compose this should resolve to the API service directly.
CLAWBOARD_SERVER_API_BASE=http://api:8000
# Optional fallback token used by Next.js /api proxy when header is missing.
# Leave blank to forward only client-provided X-Clawboard-Token.
# CLAWBOARD_SERVER_API_TOKEN=

# Optional: public/browser-facing UI URL (for installer summaries and docs).
CLAWBOARD_PUBLIC_WEB_URL=http://localhost:3010

# Web UI dev mode (hot reload)
# 0 = production-style container (default), 1 = Next.js dev server with hot reload.
CLAWBOARD_WEB_HOT_RELOAD=1

# Extra allowed dev origins/hosts for the Next dev server (CSV).
# You can provide full origins (e.g. https://my-host.ts.net:3010) or hostnames (e.g. my-host.ts.net).
CLAWBOARD_ALLOWED_DEV_ORIGINS=

# OpenClaw gateway base URL for the classifier worker (docker).
OPENCLAW_BASE_URL=http://host.docker.internal:18789
# Optional: explicit websocket endpoint for backend gateway RPC.
# Use ws:// for local/tailscale direct gateway. Use wss:// only when gateway is
# fronted by TLS and presents a valid certificate for the host.
# OPENCLAW_WS_URL=ws://host.docker.internal:18789
# Optional: override Host header for websocket connect.
# For local loopback gateways (including host.docker.internal), using 127.0.0.1
# avoids noisy "non-local Host header" gateway warnings.
# OPENCLAW_GATEWAY_HOST_HEADER=127.0.0.1
# Optional: websocket transport tuning for gateway RPC stability.
# OPENCLAW_GATEWAY_WS_CONNECT_TIMEOUT_SECONDS=30
# OPENCLAW_GATEWAY_WS_RECV_TIMEOUT_SECONDS=45
# OPENCLAW_GATEWAY_WS_PING_INTERVAL_SECONDS=20
# OPENCLAW_GATEWAY_WS_PING_TIMEOUT_SECONDS=20
# OPENCLAW_GATEWAY_WS_CLOSE_TIMEOUT_SECONDS=8
# Token from ~/.openclaw/openclaw.json -> gateway.auth.token
OPENCLAW_GATEWAY_TOKEN=your-openclaw-gateway-token
# Enable device auth by mounting ~/.openclaw into the API container.
OPENCLAW_GATEWAY_USE_DEVICE_AUTH=1
# Minimum connect scopes for gateway clients; write is required for chat.send.
OPENCLAW_GATEWAY_SCOPES=operator.read,operator.write
OPENCLAW_GATEWAY_IDENTITY_DIR=/root/.openclaw
# Optional: OpenClaw chat dispatch timeout (seconds) for gateway chat.send (clamped 5..604800).
# OPENCLAW_CHAT_TIMEOUT_SECONDS=120
# Durable Clawboard -> gateway dispatch queue worker (enabled by default).
# Set OPENCLAW_CHAT_DISPATCH_DISABLE=1 only for emergency debugging.
# OPENCLAW_CHAT_DISPATCH_DISABLE=1
OPENCLAW_CHAT_DISPATCH_POLL_SECONDS=1.5
OPENCLAW_CHAT_DISPATCH_WORKERS=2
OPENCLAW_CHAT_DISPATCH_HOT_WINDOW_SECONDS=900
OPENCLAW_CHAT_DISPATCH_STALE_PROCESSING_SECONDS=180
OPENCLAW_CHAT_DISPATCH_MAX_RETRY_DELAY_SECONDS=300
# 0 = unlimited retries (recommended for durable delivery).
OPENCLAW_CHAT_DISPATCH_MAX_ATTEMPTS=0
OPENCLAW_CHAT_DISPATCH_RECOVERY_LOOKBACK_SECONDS=604800
OPENCLAW_CHAT_DISPATCH_RECOVERY_MAX_ROWS=5000
OPENCLAW_CHAT_DISPATCH_RECOVERY_INTERVAL_SECONDS=300
# Auto-quarantine stale queue rows so synthetic/E2E backlog cannot block fresh traffic forever.
# Set OPENCLAW_CHAT_DISPATCH_AUTO_QUARANTINE_DISABLE=1 to turn this off.
# OPENCLAW_CHAT_DISPATCH_AUTO_QUARANTINE_DISABLE=1
OPENCLAW_CHAT_DISPATCH_AUTO_QUARANTINE_SECONDS=21600
OPENCLAW_CHAT_DISPATCH_AUTO_QUARANTINE_LIMIT=200
# 1 = quarantine only synthetic/test-like rows; 0 = include all stale rows.
OPENCLAW_CHAT_DISPATCH_AUTO_QUARANTINE_SYNTHETIC_ONLY=1
# Optional: per-RPC timeout for gateway calls (chat.send/chat.abort connect + request/response).
# Keep this much lower than OPENCLAW_CHAT_TIMEOUT_SECONDS so a stalled websocket handshake
# does not block the per-session dispatch lane for hours.
OPENCLAW_CHAT_SEND_RPC_TIMEOUT_SECONDS=45
# Optional: incident mitigation. After chat.send "started", wait up to this many seconds
# for any non-user progress for the request before probing in-flight status.
# Set to 0 to disable abort+retry recovery path (recommended for multi-hour/day runs).
# Required: no backend fallback; set explicitly.
OPENCLAW_CHAT_IN_FLIGHT_PROBE_SECONDS=0
# Optional: polling interval while checking request progress during the in-flight probe window.
OPENCLAW_CHAT_IN_FLIGHT_PROBE_POLL_SECONDS=5
# Optional: after abort+retry starts, wait this many seconds for any non-user progress.
# This is a post-retry grace window; if no progress appears, Clawboard marks the request failed.
# Required: no backend fallback; set explicitly.
OPENCLAW_CHAT_IN_FLIGHT_RETRY_GRACE_SECONDS=120
# Optional: when assistant rows arrive without requestId, look this far back for unresolved user sends
# in the same logical session lineage. Increase for long-running multi-agent handoffs.
# Required: no backend fallback; set explicitly.
OPENCLAW_REQUEST_ATTRIBUTION_LOOKBACK_SECONDS=1209600
# Optional: cap candidate unresolved request rows scanned during attribution.
# Required: no backend fallback; set explicitly.
OPENCLAW_REQUEST_ATTRIBUTION_MAX_CANDIDATES=24
# Optional: after a *successful* gateway call, wait this many seconds for assistant logs to land in Clawboard
# via the logger plugin before starting watchdog polling. Set to 0 to disable watchdog checks.
OPENCLAW_CHAT_ASSISTANT_LOG_GRACE_SECONDS=30
# Optional: watchdog polling interval while waiting for assistant output.
OPENCLAW_CHAT_ASSISTANT_LOG_POLL_SECONDS=30
# Optional: warn only after this many seconds of no non-user activity for the request/session.
# This prevents false warnings for long-running agent/tool executions.
OPENCLAW_CHAT_ASSISTANT_LOG_IDLE_SECONDS=900
# Optional: throttle repeated watchdog missing-output warnings for the same request.
OPENCLAW_CHAT_ASSISTANT_LOG_WARN_INTERVAL_SECONDS=1800
# Optional: on API startup, recover unresolved watchdog checks from user logs this far back.
OPENCLAW_CHAT_ASSISTANT_LOG_RECOVERY_LOOKBACK_SECONDS=604800
# Optional: cap startup recovery scan size for very large log tables.
OPENCLAW_CHAT_ASSISTANT_LOG_RECOVERY_MAX_ROWS=5000
# Optional: if requestId/messageId activity is missing, allow unlabeled session activity for
# this many seconds after send before treating it as unrelated noise.
OPENCLAW_CHAT_ASSISTANT_LOG_SESSION_ACTIVITY_WINDOW_SECONDS=300
# Optional: throttle watchdog-triggered one-shot history backfills for stuck requests.
OPENCLAW_CHAT_ASSISTANT_LOG_BACKFILL_BASE_SECONDS=60
OPENCLAW_CHAT_ASSISTANT_LOG_BACKFILL_MAX_SECONDS=3600
OPENCLAW_CHAT_ASSISTANT_LOG_BACKFILL_STATE_TTL_SECONDS=86400
# Optional: fallback background sync to ingest gateway session history into Clawboard state.
# This is restart-safe (cursor-backed) and catches messages even if logger/watchdog missed them.
OPENCLAW_GATEWAY_HISTORY_SYNC_POLL_SECONDS=60
OPENCLAW_GATEWAY_HISTORY_SYNC_ACTIVE_MINUTES=10080
OPENCLAW_GATEWAY_HISTORY_SYNC_SESSION_LIMIT=48
# Optional: disable sessions.list discovery and rely on cursor/recent-log/unresolved seeds.
# This lowers gateway pressure on large installations while preserving durable backfill.
# OPENCLAW_GATEWAY_HISTORY_SYNC_SESSIONS_LIST_DISABLE=1
# Optional: hard cap for one sync cycle; remaining sessions defer to next cycle.
OPENCLAW_GATEWAY_HISTORY_SYNC_CYCLE_BUDGET_SECONDS=20
# Optional: additionally sample this many persisted cursor sessions per cycle.
# Helps keep long-lived threads durable even when sessions.list is truncated/unhealthy.
OPENCLAW_GATEWAY_HISTORY_SYNC_CURSOR_SEED_LIMIT=8
# Optional: seed additional sessionKeys from recent conversation logs (for first-run recovery
# before cursor sync has ever succeeded).
OPENCLAW_GATEWAY_HISTORY_SYNC_LOG_SEED_LIMIT=8
# Optional: lookback window for recent log seeding.
OPENCLAW_GATEWAY_HISTORY_SYNC_LOG_SEED_LOOKBACK_SECONDS=1209600
# Optional: prioritize this many unresolved request sessions per cycle (user send present,
# but no assistant/system terminal log yet).
OPENCLAW_GATEWAY_HISTORY_SYNC_UNRESOLVED_SEED_LIMIT=16
# Optional: lookback window for unresolved-request session seeding.
OPENCLAW_GATEWAY_HISTORY_SYNC_UNRESOLVED_LOOKBACK_SECONDS=604800
# Optional: hard timeout per gateway sessions.list/chat.history RPC during sync.
OPENCLAW_GATEWAY_HISTORY_SYNC_RPC_TIMEOUT_SECONDS=15
# Optional: max adaptive backoff when sync is degraded/failing.
OPENCLAW_GATEWAY_HISTORY_SYNC_MAX_BACKOFF_SECONDS=600
# Optional: per-session cooldown for repeated chat.history failures.
OPENCLAW_GATEWAY_HISTORY_SYNC_SESSION_BACKOFF_BASE_SECONDS=30
OPENCLAW_GATEWAY_HISTORY_SYNC_SESSION_BACKOFF_TTL_SECONDS=86400
# Optional: retries for transient transport/connectivity errors before surfacing RPC failure.
OPENCLAW_GATEWAY_HISTORY_SYNC_TRANSPORT_RETRY_ATTEMPTS=2
OPENCLAW_GATEWAY_HISTORY_SYNC_TRANSPORT_RETRY_BASE_SECONDS=1.5
OPENCLAW_GATEWAY_HISTORY_SYNC_HISTORY_LIMIT=120
OPENCLAW_GATEWAY_HISTORY_SYNC_OVERLAP_SECONDS=120
# Optional: dedicated token for history-sync read calls.
# Use this when chat send uses a write-only token and history sync needs operator.read.
# OPENCLAW_GATEWAY_HISTORY_TOKEN=
# Optional: force history sync device-auth mode (1=true, 0=false). Leave unset to inherit
# OPENCLAW_GATEWAY_USE_DEVICE_AUTH and auto-fallback to non-device auth on pairing/scope errors.
# OPENCLAW_GATEWAY_HISTORY_USE_DEVICE_AUTH=
# Optional: allow one automatic retry without device auth when read calls fail due pairing/scope.
# OPENCLAW_GATEWAY_HISTORY_SYNC_RETRY_WITHOUT_DEVICE_AUTH=1
# Optional: on missing operator.read scope, retry history RPCs with operator.write scope.
# Useful for installs where the gateway token is write-scoped but still allowed to read history.
# OPENCLAW_GATEWAY_HISTORY_SYNC_RETRY_WITH_WRITE_SCOPE=1
# Optional: throttle interval (seconds) for sync failure system logs.
# OPENCLAW_GATEWAY_HISTORY_SYNC_ERROR_LOG_INTERVAL_SECONDS=900
# Optional: disable fallback history sync (1=disable, default enabled when gateway token is set)
# OPENCLAW_GATEWAY_HISTORY_SYNC_DISABLE=1

# OpenClaw clawboard-logger plugin: context injection tuning.
#
# These values do NOT affect the Clawboard server directly. They are consumed by
# scripts/bootstrap_openclaw.sh when writing the OpenClaw plugin config
# (plugins.entries.clawboard-logger.config.*).
#
# Modes:
# - auto: cheap continuity by default; semantic recall when needed (recommended)
# - cheap: fastest; no semantic recall
# - full: always semantic recall
# - patient: deeper recall (server may use larger bounded limits; allow more time)
CLAWBOARD_LOGGER_CONTEXT_MODE=auto
CLAWBOARD_LOGGER_CONTEXT_FETCH_TIMEOUT_MS=3000
CLAWBOARD_LOGGER_CONTEXT_FETCH_RETRIES=1
# Ordered fallback modes used when primary mode fails (comma-separated).
CLAWBOARD_LOGGER_CONTEXT_FALLBACK_MODES=full,auto,cheap
CLAWBOARD_LOGGER_CONTEXT_MAX_CHARS=2200
# Context cache to smooth transient gateway/API hiccups.
CLAWBOARD_LOGGER_CONTEXT_CACHE_TTL_MS=45000
CLAWBOARD_LOGGER_CONTEXT_CACHE_MAX_ENTRIES=120
CLAWBOARD_LOGGER_CONTEXT_USE_CACHE_ON_FAILURE=1
# 1: allow OpenClaw memory_search/memory_get during normal turns.
# 0 (default): prefer Clawboard retrieval context without OpenClaw memory search.
CLAWBOARD_LOGGER_ENABLE_OPENCLAW_MEMORY_SEARCH=0
# Optional: clawboard-logger in-memory requestId cache for unlabeled follow-up events.
# Keep long enough for cross-agent handoffs spanning hours/days.
# Plugin fallback exists for safety if unset; set explicitly in production.
OPENCLAW_REQUEST_ID_TTL_SECONDS=604800
OPENCLAW_REQUEST_ID_MAX_ENTRIES=5000
# Subagent board-scope persistence from DB (hours). Long-running subagents inherit task/topic for this long. 1–168.
CLAWBOARD_BOARD_SCOPE_SUBAGENT_TTL_HOURS=48

# Classifier worker cadence + retry policy
CLASSIFIER_INTERVAL_SECONDS=10
CLASSIFIER_MAX_ATTEMPTS=3
# Suppress repeated scope resolution 404 churn for stale task/topic ids.
CLASSIFIER_MISSING_ENTITY_TTL_SECONDS=1800
CLASSIFIER_MISSING_ENTITY_CACHE_MAX=5000

# Optional: classifier decision audit log (JSONL). Useful for tuning and debugging.
# In docker-compose, /data is bind-mounted to ./data on the host (purge-state.sh archives/purges it).
# CLASSIFIER_AUDIT_PATH=/data/classifier-audit.jsonl

# Optional: topic/task digests (compressed memory). System-managed; does not affect embeddings.
# Default is enabled, but you can disable in prod if you want strictly manual memory.
# CLASSIFIER_DIGEST_ENABLED=1
# Minimum seconds between digest refresh attempts for the same topic/task.
# CLASSIFIER_DIGEST_MIN_SECONDS=900
# Max topics+tasks to update per classifier cycle (keep low to avoid churn).
# CLASSIFIER_DIGEST_MAX_PER_CYCLE=2
# How many recent conversation logs to consider when generating digests.
# CLASSIFIER_DIGEST_TOPIC_LOOKBACK_LOGS=120
# CLASSIFIER_DIGEST_TASK_LOOKBACK_LOGS=90
# Maximum digest size stored on Topic/Task.
# CLASSIFIER_DIGEST_MAX_CHARS=1800

# Event stream buffer size (SSE replay)
CLAWBOARD_EVENT_BUFFER=500
# Optional: per-subscriber SSE queue depth (defaults to CLAWBOARD_EVENT_BUFFER)
# CLAWBOARD_EVENT_SUBSCRIBER_QUEUE=500

# Optional: async ingest queue
# CLAWBOARD_INGEST_MODE=queue
# CLAWBOARD_QUEUE_POLL_SECONDS=1.5
# CLAWBOARD_QUEUE_BATCH=25

# Optional: path to the reindex queue jsonl file (defaults to ./data/reindex-queue.jsonl)
# CLAWBOARD_REINDEX_QUEUE_PATH=./data/reindex-queue.jsonl

# Optional: background snooze worker (re-activates items when snoozedUntil expires)
# CLAWBOARD_DISABLE_SNOOZE_WORKER=0
# CLAWBOARD_SNOOZE_POLL_SECONDS=15

# Classifier context and embeddings (optional)
CLASSIFIER_WINDOW_SIZE=24
CLASSIFIER_LOOKBACK_LOGS=80
CLASSIFIER_TOPIC_SIM_THRESHOLD=0.78
CLASSIFIER_TASK_SIM_THRESHOLD=0.80
CLASSIFIER_EMBED_MODEL=BAAI/bge-small-en-v1.5
# Note: if you override CLASSIFIER_EMBED_MODEL, keep CLAWBOARD_VECTOR_MODEL in sync for best semantic search.
# CLAWBOARD_VECTOR_MODEL=BAAI/bge-small-en-v1.5
# Semantic search ingestion policy:
# 0 (default): exclude action logs that are tool call/result/error traces from embeddings + semantic recall.
# 1: include tool activity logs in semantic indexing/retrieval.
CLAWBOARD_SEARCH_INCLUDE_TOOL_CALL_LOGS=0
# Search engine mode:
# auto (default): use fast path for low-signal/very-large searches, hybrid for high-signal queries.
# hybrid: always run bm25+lexical+rrf+rerank (qdrant-boosted if available).
# fast: always run lexical-fast.
CLAWBOARD_SEARCH_MODE=auto
# Dense retrieval toggle:
# 1 (default): allow query embeddings + qdrant dense candidate recall.
# 0: skip dense recall and run sparse hybrid only (bm25+lexical+rrf+rerank) for faster/leaner installs.
# CLAWBOARD_SEARCH_ENABLE_DENSE=1
# Backward compatibility toggle (legacy): 1 implies hybrid when CLAWBOARD_SEARCH_MODE is unset.
CLAWBOARD_SEARCH_ENABLE_HEAVY_SEMANTIC=0
# Search safety caps (protect API/gateway under aggressive UI search workloads).
# CLAWBOARD_SEARCH_EFFECTIVE_LIMIT_TOPICS=180
# CLAWBOARD_SEARCH_EFFECTIVE_LIMIT_TASKS=360
# CLAWBOARD_SEARCH_EFFECTIVE_LIMIT_LOGS=600
# CLAWBOARD_SEARCH_WINDOW_MULTIPLIER=4
# CLAWBOARD_SEARCH_WINDOW_MIN_LOGS=1200
# CLAWBOARD_SEARCH_WINDOW_MAX_LOGS=5000
# CLAWBOARD_SEARCH_WINDOW_MULTIPLIER_CAP=2
# CLAWBOARD_SEARCH_WINDOW_MIN_LOGS_CAP=480
# CLAWBOARD_SEARCH_WINDOW_MAX_LOGS_CAP=900
# CLAWBOARD_SEARCH_LOG_FETCH_LIMIT_CAP=1200
# CLAWBOARD_SEARCH_LOG_SUMMARY_FETCH_CHARS=520
# CLAWBOARD_SEARCH_SINGLE_TOKEN_WINDOW_MAX_LOGS=900
# CLAWBOARD_SEARCH_CONCURRENCY_LIMIT=2
# CLAWBOARD_SEARCH_CONCURRENCY_WAIT_SECONDS=0.15
# CLAWBOARD_SEARCH_LOG_CONTENT_MATCH_CLIP_CHARS=2400
# CLAWBOARD_SEARCH_SOURCE_TOPK_MULTIPLIER=6
# CLAWBOARD_SEARCH_SOURCE_TOPK_MIN=120
# CLAWBOARD_SEARCH_SOURCE_TOPK_MAX=960
# CLAWBOARD_RERANK_CHUNKS_PER_DOC=6
# CLAWBOARD_SEARCH_EMBED_QUERY_CACHE_SIZE=256
# CLAWBOARD_SEARCH_AUTO_HYBRID_MIN_QUERY_TOKENS=2
# CLAWBOARD_SEARCH_AUTO_HYBRID_MIN_QUERY_CHARS=6
# CLAWBOARD_SEARCH_AUTO_HYBRID_MAX_LOG_DOCS=520
# CLAWBOARD_SEARCH_HYBRID_PREFILTER_MULTIPLIER=4
# CLAWBOARD_SEARCH_HYBRID_MAX_TOPIC_DOCS=320
# CLAWBOARD_SEARCH_HYBRID_MAX_TASK_DOCS=460
# CLAWBOARD_SEARCH_HYBRID_MAX_LOG_DOCS=380
# CLAWBOARD_SEARCH_RESULT_CACHE_TTL_SECONDS=10
# CLAWBOARD_SEARCH_RESULT_CACHE_MAX_KEYS=96
# CLAWBOARD_SEARCH_DIRECT_LABEL_EXACT_BOOST=0.38
# CLAWBOARD_SEARCH_DIRECT_LABEL_PREFIX_BOOST=0.2
# CLAWBOARD_SEARCH_DIRECT_LABEL_COVERAGE_BOOST=0.16
# Qdrant-primary dense retrieval policy:
# 1: require Qdrant for dense retrieval (recommended for production)
CLAWBOARD_VECTOR_REQUIRE_QDRANT=1

# Embeddings/search index (Qdrant is enabled by default in docker-compose)
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION=clawboard_embeddings
QDRANT_DIM=384
# Optional: if your Qdrant uses API key auth
# QDRANT_API_KEY=

# Optional: enrich classifier with OpenClaw memory snippets
# Mount the host memory dir into the classifier container and point to the sqlite file.
# Example volume: ~/.openclaw/memory:/data/openclaw-memory:ro
OPENCLAW_MEMORY_DB_PATH=/data/openclaw-memory/main.sqlite
OPENCLAW_MEMORY_DB_FALLBACK=/data/openclaw-memory/main.sqlite
OPENCLAW_MEMORY_MAX_HITS=6

# Optional: restrict CORS
# CLAWBOARD_CORS_ORIGINS=http://localhost:3010

# Optional: trust X-Forwarded-For / X-Real-IP from your reverse proxy.
# Leave unset (default) unless you control the proxy boundary.
# CLAWBOARD_TRUST_PROXY=0

# Optional (docker web-dev profile): tweak file watching (polling is slower but reliable on Docker Desktop).
# CLAWBOARD_WEB_WATCHPACK_POLLING=true
# CLAWBOARD_WEB_WATCHPACK_POLLING_INTERVAL=1000
# Optional (docker web-dev profile): prewarm heavy routes after startup to avoid slow first-hit compiles.
# CLAWBOARD_WEB_DEV_PREWARM=true
# CLAWBOARD_WEB_DEV_PREWARM_PATHS="/ /u /graph /log /stats"
# CLAWBOARD_WEB_DEV_PREWARM_TIMEOUT_MS=180000
# CLAWBOARD_WEB_DEV_READY_PROBE_PATH=/favicon.ico

# Optional: server-side precompiled response snapshots for heavy endpoints (/api/clawgraph, /api/changes, /api/metrics).
# Enabled by default for both dev and prod. Disable only when debugging cache behavior.
# CLAWBOARD_PRECOMPILE_ENABLED=1
# CLAWBOARD_PRECOMPILE_TTL_SECONDS=8
# CLAWBOARD_PRECOMPILE_MAX_KEYS=32
# CLAWBOARD_PRECOMPILE_WARM_ON_STARTUP=1
# CLAWBOARD_PRECOMPILE_WARM_LISTENER_ENABLED=1
# CLAWBOARD_PRECOMPILE_WARM_MIN_INTERVAL_SECONDS=1.5
